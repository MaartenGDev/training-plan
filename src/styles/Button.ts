import {StyleSheet} from "react-native";

export default StyleSheet.create({
    wrapper: {
        backgroundColor: '#48bb78',
        borderRadius: 4,
    },
    label: {
        color: "#ffffff",
        fontWeight: 'bold',
        fontSize: 14,
        paddingTop: 8,
        paddingBottom: 8,
        textAlign: 'center'

    }
});
